# Dependencies for Stock_Price data pullers.
# Install with:  pip install -r requirements.txt
# (yfinance pulls in curl_cffi, multitasking, peewee, lxml, etc. transitively.)

yfinance>=1.5
pandas>=3.0
numpy>=2.0
pyarrow>=15          # required by pull_daily.py for parquet output
requests>=2.31